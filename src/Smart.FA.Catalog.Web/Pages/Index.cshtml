@page
@model IndexModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Index";
    ViewData["ToolBarTitle"] = "Add a new training";
}


<smart-card>
    <smart-card-body>
        <smart-card-title text="Hi @Model.Identity!.ToString()"></smart-card-title>
    </smart-card-body>
</smart-card>
